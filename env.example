# Server
PORT=3000

# SMTP configuration
# For Gmail, you may need an App Password and host=smtp.gmail.com, port=465, secure=true
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=

# Default From address (optional). Falls back to SMTP_USER
MAIL_FROM=


